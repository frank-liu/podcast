// T010 - Worker orchestration (failing skeleton)
describe('T010 - Digest worker (failing skeleton)', () => {
  test('TODO: run digest job with mocked adapters and produce Digest with audio_url', () => {
    throw new Error('T010 test skeleton - implement worker integration test');
  });
});
