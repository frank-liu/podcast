// T008 - Duration budget planner (failing skeleton)
describe('T008 - Budget planner (failing skeleton)', () => {
  test('TODO: given per-char estimates returns per-message budgets to fit total <= 600s', () => {
    throw new Error('T008 test skeleton - implement budget planner');
  });
});
